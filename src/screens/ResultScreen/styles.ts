import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  rootContainer: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'center',
    margin: 20,
  },

  headlineText: {
    fontSize: 28,
    textAlign: 'center',
    fontWeight: 'bold',
  },

  answerReportContainer: {
    marginVertical: 60,
  },

  playAgainTextStyle: {
    fontWeight: 'bold',
    fontSize: 28,
  },
});
